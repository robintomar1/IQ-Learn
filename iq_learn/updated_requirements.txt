# Python 3.9+ is highly recommended for PyTorch 2.x and SB3 2.x
#
# --- Core Deep Learning (PyTorch) ---
# NOTE: Check PyTorch website for the exact command for your CUDA version.
# These lines assume a CPU-only installation for compatibility:
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# --- Reinforcement Learning ---
stable-baselines3>=2.2.1
# Gymnasium with all environment families (classic_control, atari, box2d, toy_text, etc.)
gymnasium[all]>=0.29.1,<1.0.0
# Atari ROMs (required to run Atari envs; install after gymnasium[all])
gymnasium[accept-rom-license]
# For CartPole/classic_control rendering (e.g. render_mode="rgb_array")
pygame

# --- Logging and Utilities ---
tensorboard>=2.16.0
wandb
tqdm>=4.66.1
termcolor>=2.0.0

# --- Other Requirements ---
hydra-core>=1.3.2
opencv-python>=4.10.0
tornado
trackio
tensorboardX
grad-cam
# trackio throws error if the python version is 3.8 or 3.9 works with 3.10

# --- Faster vectorised Atari environments (C++ backend, no subprocess IPC) ---
# envpool 0.8.4 requires JAX < 0.6.0; pin both together
envpool==0.8.4
jax==0.4.38
jaxlib==0.4.38